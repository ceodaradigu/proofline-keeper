[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "proofline-keeper"
version = "0.1.0"
description = "Evidence-gated KeeperHub execution for AI agents"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]

